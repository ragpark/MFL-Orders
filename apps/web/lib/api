import axios from 'axios';
export const api = axios.create({ baseURL: process.env.NEXT_PUBLIC_API_BASE_URL || 'http://localhost:4000/api' });
export async function getJson<T>(url: string): Promise<T> { const res = await api.get<T>(url); return res.data; }
